import MemorySlots from "../../models/Filter/MemorySlots";

const MEMORY_SLOTS = [
   new MemorySlots(1, 0, "0"),
   new MemorySlots(2, 1, "1"),
   new MemorySlots(3, 2, "2"),
   new MemorySlots(4, 4, "4"),
];

export default MEMORY_SLOTS;
