class CategoryItem {
   constructor(id, name, slug, icon, photo) {
      this.id = id;
      this.name = name;
      this.slug = slug;
      this.icon = icon;
      this.photo = photo;
   }
}
export default CategoryItem;
